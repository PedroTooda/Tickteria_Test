import { Scenario } from '../scenario.contract';

export const scenario: Scenario = {
  name: 'amount-mismatch',
  async run() {
    throw new Error('cenário amount-mismatch não implementado');
  },
};
