import { Scenario } from '../scenario.contract';

export const scenario: Scenario = {
  name: 'duplicate-parallel',
  async run() {
    throw new Error('cenário duplicate-parallel não implementado');
  },
};
